export const YEAR_ELEMENT_ID = "current-year";

/**
 * 默认后端服务端口。
 * @type {number}
 */
export const DEFAULT_BACKEND_PORT = 8000;

/**
 * 本地扫描助手默认端口。
 * @type {number}
 */
export const LOCAL_SCANNER_PORT = 47832;

export const IMAGE_PREVIEW_OVERLAY_ID = "image-preview-overlay";

